import heap.Heap;

package lab7;

public class Components {

    heap = new Heap();

    public static void countFrequenies(String string){
        for (i = 0; i < string.length)
        throw new UnsupportedOperationException();
    }

    public static hNode buildTree(Heap heap){}

    public static Decode(hNode rootNode, String codedString){}

    public static Encode(hNode rootNode, String inputString){}


    public static void main(String[] args) {
        System.out.println(new Components().getGreeting());
    }

    public class hNode{

        public Entry entry;
        public hNode parent;
        public hNode leftChild;
        public hNode rightChild; 

        public hNode(Entry e, hNode p, hNode l, hNode r){
            e = entry;
            p = parent;
            l = leftchild;
            r = rightChild;
        }
    
    }

    

}